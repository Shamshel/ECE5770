//Wi-Fi_controller.c

#include "Tasks/Wi-Fi_controller.h"

//static unsigned int bufSize = 0;
//static char msgBuffer[MESSAGE_SIZE];

static bool responded = true;

void WiFiSendCommand(const char* cmd)
{
  MPI_send_message(WIFI_CONTROLLER_PID, WIFI_DRIVER_PID, OS_strlen(cmd), (void*)cmd);

  responded = false;  

}

void WiFiCheckForResponse(const char* resp)
{
  if(MPI_check_available(WIFI_CONTROLLER_PID) == true)
    {
      

    }

}

void WiFiSendCommandCheck(const char* cmd, const char* resp)
{
  //int incomingPID;
  int cmdLen = OS_strlen(cmd);

  MPI_send_message(WIFI_CONTROLLER_PID, WIFI_DRIVER_PID, cmdLen, (void*)cmd);

}

void WiFi_controller_init()
{
  //inits currently all handled by driver code
  

}

void WiFi_controller_run()
{
  

}

